<UserControl x:Class="Jacobi.Vst.Samples.MidiNoteSampler.UI.InnerPluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Jacobi.Vst.Samples.MidiNoteSampler.UI"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <ListBox x:Name="listBox" ItemsSource="{Binding GeneratorKnobs}" d:ItemsSource="{d:SampleData ItemCount=5}" Background="Black" Foreground="White">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <CheckBox Content="{Binding Label}" IsChecked="{Binding IsValue}" Foreground="White"/>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <ListBox x:Name="listBox1" ItemsSource="{Binding GeneralKnobs}" Grid.Column="1" d:ItemsSource="{d:SampleData ItemCount=5}" Background="Black" Foreground="White">
      <ListBox.ItemTemplate>
        <DataTemplate >
          <local:ParameterKnob HorizontalAlignment="Stretch"/></DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

  </Grid>
</UserControl>
